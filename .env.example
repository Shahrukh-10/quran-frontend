# Public site URL — used for canonical URLs, sitemap, OG tags.
# In local dev, http://localhost:3000. In production, your custom domain.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Site name (shown in OG tags and titles). Change when you pick the final brand.
NEXT_PUBLIC_SITE_NAME="Islamic Website"
